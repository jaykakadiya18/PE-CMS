import { blockContent } from './blockContent'
import companyMedia from './media'
import workflow from './workflow'

export { blockContent, companyMedia, workflow }
export const sharedSchemas = [blockContent, companyMedia, workflow]